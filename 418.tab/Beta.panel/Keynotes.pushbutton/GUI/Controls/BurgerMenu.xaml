<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ControlTemplate x:Key="BurgerMenuTemplate" TargetType="ContentControl">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- En-tête du menu -->
            <Border Grid.Row="0" Background="{DynamicResource MicaBackgroundBrush}" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource ControlBorderBrush}" Padding="16">
                <Grid>
                    <TextBlock Text="Paramètres" Style="{DynamicResource SubHeaderTextBlockStyle}" VerticalAlignment="Center"/>
                    <Button Name="CloseBurgerButton" 
                            Content="×" 
                            FontSize="24"
                            Width="32" Height="32"
                            HorizontalAlignment="Right"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Foreground="{DynamicResource TextPrimaryBrush}"/>
                </Grid>
            </Border>
            
            <!-- Contenu du menu -->
            <Grid Grid.Row="1">
                <StackPanel Margin="16">
                    <TextBlock Text="Apparence" Style="{DynamicResource BodyTextBlockStyle}" Margin="0,0,0,8"/>
                    <CheckBox Name="DarkModeToggle" Content="Mode Sombre" Margin="0,0,0,16"/>

                    <TextBlock Text="Fichier Keynotes" Style="{DynamicResource BodyTextBlockStyle}" Margin="0,0,0,8"/>
                    <Button Name="LoadKeynoteButton" Content="Charger un fichier..." HorizontalAlignment="Stretch"/>
                    <TextBlock Name="CurrentKeynotePath" 
                               Text="Aucun fichier chargé" 
                               Style="{DynamicResource BodyTextBlockStyle}" 
                               FontSize="12" 
                               Foreground="{DynamicResource TextSecondaryBrush}" 
                               TextWrapping="Wrap" 
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </ControlTemplate>
</ResourceDictionary>
