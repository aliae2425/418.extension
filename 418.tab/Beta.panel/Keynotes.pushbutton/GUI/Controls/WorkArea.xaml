<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <ControlTemplate x:Key="WorkAreaTemplate" TargetType="ContentControl">
    <Border Style="{DynamicResource CardStyle}" Margin="0">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <Button Grid.Column="0"
                  Content="{DynamicResource filterIcon}"
                  Width="36" Height="36"
                  Padding="0"
                  Margin="0,0,10,0"
              />

          <Grid Grid.Column="1">
            <TextBox x:Name="search_tb"
                    Padding="10,0,34,0"
                    
                    VerticalContentAlignment="Center"/>

            <Button x:Name="clrsearch_b"
                    Visibility="Collapsed"
                    Width="30" Height="30"
                    HorizontalAlignment="Right"
                    Margin="0,2,2,2"
                    Background="{DynamicResource TransparentBrush}"
                    BorderThickness="0"
                    Style="{DynamicResource ClearButton}"
                  >
              <Button.Content>
                <Canvas Width="18" Height="18" VerticalAlignment="Center" HorizontalAlignment="Center">
                  <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z"
                        Fill="{DynamicResource TextSecondaryBrush}"/>
                </Canvas>
              </Button.Content>
            </Button>
          </Grid>
        </Grid>

        <ComboBox x:Name="categories_tv"
                  Grid.Row="1"
                  Margin="0,12,0,12"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ItemTemplate="{DynamicResource treeViewItem}"
                  ItemContainerStyle="{DynamicResource comboItemStyle}"
                  />

        <TreeView x:Name="keynotes_tv"
                  Grid.Row="2"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ItemTemplate="{DynamicResource treeViewItem}"
                  ItemContainerStyle="{DynamicResource treeItemStyle}"/>
      </Grid>
    </Border>
  </ControlTemplate>

</ResourceDictionary>
