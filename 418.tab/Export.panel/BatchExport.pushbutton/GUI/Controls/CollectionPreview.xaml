<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!--
    CollectionPreviewTemplate

    Où c'est utilisé
    - Hébergé par CollectionPreviewHost dans Views/index.xaml.

    Rôle
    - Afficher un récapitulatif des collections + feuilles à exporter.
    - Fournir la barre de progression, l'état, et le bouton "Exporter".

    Nommage attendu (utilisé côté Python)
    - ExportProgressBar : progression globale.
    - PreviewCounterText : compteur collections/éléments.
    - CollectionGrid : ListView groupée (GroupStyle/Expander).
    - ExportStatusText : message d'état (erreur/warn/succès).
    - ExportButton : action principale (activée/désactivée côté code).

    Bindings importants
    - Group Header bouton "🔃" : appelle ManageOrderCommand exposé par la Window
      (RelativeSource AncestorType=Window).
    - Les Items (dictionnaires) sont attendus avec des clés comme:
      [CollectionIsExported], [CollectionPreviewName], [SheetCount], [CollectionExportInfo],
      [IsCombined], [SheetNumber], [PreviewNom], [Format], [Size], [Orientation], [Progress], [ProgressColor].
  -->
  <!-- 
       TEMPLATE PRINCIPAL : CollectionPreviewTemplate
       Ce template définit l'interface utilisateur pour la prévisualisation des collections à exporter.
       Il est utilisé par le composant CollectionPreviewComponent.
  -->
  <ControlTemplate x:Key="CollectionPreviewTemplate" TargetType="ContentControl">
    <ControlTemplate.Resources>
      <!-- Convertisseur pour afficher/masquer des éléments basés sur un booléen -->
      <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

      <!-- CheckBox style Fluent compact pour l'en-tête de groupe -->
      <Style x:Key="FluentHeaderCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="CheckBox">
              <BulletDecorator Background="Transparent" VerticalAlignment="Center">
                <BulletDecorator.Bullet>
                  <Border x:Name="CheckBorder"
                          Width="12" Height="12"
                          CornerRadius="2"
                          BorderThickness="1.5"
                          BorderBrush="{DynamicResource ControlBorderBrush}"
                          Background="{DynamicResource ControlFillBrush}">
                    <Path x:Name="CheckMark"
                          Data="M 1 4.5 L 4 7.5 L 9.5 2"
                          Stroke="White"
                          StrokeThickness="1.5"
                          StrokeStartLineCap="Round"
                          StrokeEndLineCap="Round"
                          StrokeLineJoin="Round"
                          Visibility="Collapsed"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                  </Border>
                </BulletDecorator.Bullet>
                <ContentPresenter Margin="4,0,0,0"
                                  VerticalAlignment="Center"
                                  RecognizesAccessKey="True"/>
              </BulletDecorator>
              <ControlTemplate.Triggers>
                <!-- Coché -->
                <Trigger Property="IsChecked" Value="True">
                  <Setter TargetName="CheckBorder" Property="Background" Value="{DynamicResource AccentBrush}"/>
                  <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                  <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                </Trigger>
                <!-- Survol non coché -->
                <MultiTrigger>
                  <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsChecked" Value="False"/>
                  </MultiTrigger.Conditions>
                  <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{DynamicResource ControlBorderHoverBrush}"/>
                </MultiTrigger>
                <!-- Survol coché -->
                <MultiTrigger>
                  <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsChecked" Value="True"/>
                  </MultiTrigger.Conditions>
                  <Setter TargetName="CheckBorder" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                  <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}"/>
                </MultiTrigger>
                <!-- Désactivé -->
                <Trigger Property="IsEnabled" Value="False">
                  <Setter Property="Opacity" Value="0.4"/>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>

      <!--
           STYLE DE L'EXPANDER (GROUPE)
           Personnalisation du style des groupes (Collections) dans la ListView.
           Il remplace le style par défaut pour avoir un look plus "Fluent" / moderne.
      -->
      <Style x:Key="FluentGroupExpanderStyle" TargetType="{x:Type Expander}">
        <Setter Property="Background" Value="{DynamicResource ControlFillPressedBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Expander}">
                    <Border BorderThickness="{TemplateBinding BorderThickness}" 
                            BorderBrush="{TemplateBinding BorderBrush}"
                            Background="{TemplateBinding Background}">
                        <DockPanel>
                            <!-- 
                                 TOGGLE BUTTON (EN-TÊTE DU GROUPE)
                                 C'est la partie cliquable qui permet d'ouvrir/fermer le groupe.
                            -->
                            <ToggleButton x:Name="HeaderSite"
                                          DockPanel.Dock="Top"
                                          Background="{DynamicResource TransparentBrush}"
                                          Content="{TemplateBinding Header}"
                                          ContentTemplate="{TemplateBinding HeaderTemplate}"
                                          ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
                                          Foreground="{TemplateBinding Foreground}"
                                          FontWeight="{TemplateBinding FontWeight}"
                                          FontStyle="{TemplateBinding FontStyle}"
                                          FontSize="{TemplateBinding FontSize}"
                                          FontFamily="{TemplateBinding FontFamily}"
                                          Padding="10,5"
                                          HorizontalContentAlignment="Stretch"
                                          IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <!-- Icône de flèche (Chevron) qui tourne quand le groupe est ouvert -->
                                                <Border Width="24" Height="24" Background="{DynamicResource TransparentBrush}">
                                                    <Path x:Name="arrow"
                                                          Data="M 0 0 L 4 4 L 0 8"
                                                          Stroke="{TemplateBinding Foreground}"
                                                          StrokeThickness="1.5"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          RenderTransformOrigin="0.5,0.5">
                                                        <Path.RenderTransform>
                                                            <RotateTransform Angle="0"/>
                                                        </Path.RenderTransform>
                                                    </Path>
                                                </Border>

                                                <!-- Contenu de l'en-tête (défini plus bas dans le GroupStyle) -->
                                                <ContentPresenter Grid.Column="1" 
                                                                  Content="{TemplateBinding Content}" 
                                                                  ContentTemplate="{TemplateBinding ContentTemplate}" 
                                                                  VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <!-- Changement d'apparence au survol -->
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource ControlFillHoverBrush}"/>
                                                <Setter Property="Cursor" Value="Hand"/>
                                            </Trigger>
                                            <!-- Rotation de la flèche quand le groupe est ouvert -->
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="arrow" Property="RenderTransform">
                                                    <Setter.Value>
                                                        <RotateTransform Angle="90"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            
                            <!-- 
                                 CONTENU DU GROUPE (LISTE DES FEUILLES)
                                 C'est ici que s'affichent les éléments enfants du groupe.
                            -->
                            <ContentPresenter x:Name="ExpandSite"
                                              DockPanel.Dock="Bottom"
                                              Focusable="false"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"
                                              Visibility="Collapsed"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Width="850"/>
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Affiche le contenu quand IsExpanded est vrai -->
                        <Trigger Property="IsExpanded" Value="true">
                            <Setter Property="Visibility" TargetName="ExpandSite" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <!-- Apparence quand le groupe est désactivé -->
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Background" Value="{DynamicResource TransparentBrush}"/>
              <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
      </Style>
    </ControlTemplate.Resources>
    
    <!-- CONTENEUR PRINCIPAL -->
    <StackPanel>
      <!-- ═══════════════════════════════════════════════════════════════ -->
      <!-- SECTION 1 : EN-TÊTE ET PROGRESSION -->
      <!-- ═══════════════════════════════════════════════════════════════ -->
      <TextBlock Text="Recapitulatif : " 
                  Foreground="{DynamicResource TextPrimaryBrush}"
                  FontWeight="Bold" Margin="0,0,0,6"/>
      
      <!-- Barre de progression globale de l'export -->
      <ProgressBar Name="ExportProgressBar"
                   Width="850"
                   Height="24" Minimum="0"
                   Maximum="100" Value="0" Margin="0,4,0,8"/>
      <!-- ═══════════════════════════════════════════════════════════════ -->
      <!-- SECTION 2 : TABLEAU DE PRÉVISUALISATION DES COLLECTIONS -->
      <!-- ═══════════════════════════════════════════════════════════════ -->
      <Border BorderThickness="0" Margin="0,0,0,8">
        <StackPanel>
          <!-- Texte indiquant le nombre de collections et d'éléments trouvés -->
          <TextBlock Name="PreviewCounterText"
                     Margin="0,0,0,6"
                     Foreground="{DynamicResource TextSecondaryBrush}"
                     Text="Collections: 0 • Éléments: 0"/>
          
          <!-- 
              LISTVIEW PRINCIPALE
              Affiche les feuilles regroupées par Collection.
            -->
            <ListView Name="CollectionGrid"
                Height="300"
                Width="850"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                BorderThickness="1"
                BorderBrush="{DynamicResource ControlBorderBrush}">

            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding [CollectionIsExported]}" Value="False">
                            <Setter Property="Opacity" Value="0.4"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>

            <!-- DÉFINITION DES GROUPES (COLLECTIONS) -->
            <ListView.GroupStyle>
              <GroupStyle>
                <GroupStyle.ContainerStyle>
                  <Style TargetType="{x:Type GroupItem}">
                    <Setter Property="Template">
                      <Setter.Value>
                        <ControlTemplate TargetType="{x:Type GroupItem}">
                          <!-- Utilisation du style d'Expander défini plus haut -->
                          <Expander Style="{StaticResource FluentGroupExpanderStyle}"
                                    IsExpanded="False">
                            
                            <!-- EN-TÊTE DU GROUPE (Ligne de la Collection) -->
                            <Expander.Header>
                                <Grid Height="25" Width="850">
                                    <Grid.ColumnDefinitions>
                                      <ColumnDefinition Width="150"/>   <!-- Nom de la collection -->
                                      <ColumnDefinition Width="350"/>  <!-- Nom du fichier de sortie (preview) -->
                                      <ColumnDefinition Width="70"/>   <!-- Nombre de feuilles -->
                                      <ColumnDefinition Width="200"/>    <!-- Infos d'export -->
                                      <ColumnDefinition Width="30"/>   <!-- Bouton d'ordre -->
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Colonne 1 : Nom de la collection -->
                                    <TextBlock Grid.Column="0" Text="{Binding Name}"
                                              FontStyle="Italic" VerticalAlignment="Center" Margin="5,0"/>
                                    
                                    <!-- Colonne 2 : Nom du fichier généré (si combiné) -->
                                    <TextBlock Grid.Column="1" Text="{Binding Items[0][CollectionPreviewName]}" 
                                              FontWeight="Bold" VerticalAlignment="Center" Margin="5,0" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    
                                    <!-- Colonne 3 : Nombre de feuilles dans la collection -->
                                    <TextBlock Grid.Column="2" Text="{Binding Items[0][SheetCount], StringFormat='{}{0} feuilles'}"
                                              FontStyle="Italic"  VerticalAlignment="Center" Margin="5,0"/>
                                    
                                    <!-- Colonne 4 : Flags d'export (checkboxes interactives style Fluent) -->
                                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                                        <CheckBox Content="Export"
                                                  Tag="export"
                                                  Style="{StaticResource FluentHeaderCheckBoxStyle}"
                                                  IsChecked="{Binding Items[0][CollectionExportFlag], Mode=OneWay}"
                                                  ToolTip="{Binding Items[0][ParamExport]}"
                                                  Margin="0,0,8,0"/>
                                        <CheckBox Content="Carnet"
                                                  Tag="carnet"
                                                  Style="{StaticResource FluentHeaderCheckBoxStyle}"
                                                  IsChecked="{Binding Items[0][CollectionCarnetFlag], Mode=OneWay}"
                                                  ToolTip="{Binding Items[0][ParamCarnet]}"
                                                  Margin="0,0,8,0"/>
                                        <CheckBox Content="DWG"
                                                  Tag="dwg"
                                                  Style="{StaticResource FluentHeaderCheckBoxStyle}"
                                                  IsChecked="{Binding Items[0][CollectionDwgFlag], Mode=OneWay}"
                                                  ToolTip="{Binding Items[0][ParamDwg]}"/>
                                    </StackPanel>
                                    
                                    <!-- Colonne 5 : Bouton pour gérer l'ordre (visible uniquement si combiné) -->
                                    <Button Grid.Column="4" Content="🔃" Width="25" Height="25" 
                                              Visibility="{Binding Items[0][IsCombined], Converter={StaticResource BoolToVisibilityConverter}}"
                                              Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=ManageOrderCommand}"
                                            CommandParameter="{Binding Name}"/>
                                </Grid>
                            </Expander.Header>
                            <ItemsPresenter />
                          </Expander>
                        </ControlTemplate>
                      </Setter.Value>
                    </Setter>
                  </Style>
                </GroupStyle.ContainerStyle>
              </GroupStyle>
            </ListView.GroupStyle>
            
            <!-- DÉFINITION DES COLONNES (FEUILLES INDIVIDUELLES) -->
            <ListView.View>
              <GridView AllowsColumnReorder="True">
                <!-- Colonne 1 : Numéro de la feuille -->
                <GridViewColumn Header="numéro" Width="100" DisplayMemberBinding="{Binding [SheetNumber]}"/>
                <!-- Colonne 2 : Nom du fichier généré pour cette feuille -->
                <GridViewColumn Header="nom d'export" Width="400" DisplayMemberBinding="{Binding [PreviewNom]}"/>
                <!-- Colonne 3 : Format de papier -->
                <GridViewColumn Header="Format" Width="70" DisplayMemberBinding="{Binding [Format]}"/>
                <!-- Colonne 4 : Dimensions -->
                <GridViewColumn Header="Taille" Width="70" DisplayMemberBinding="{Binding [Size]}"/>
                <!-- Colonne 5 : Orientation (Portrait/Paysage) -->
                <GridViewColumn Header="Orientation" Width="700" DisplayMemberBinding="{Binding [Orientation]}"/>
                <!-- Colonne 6 : Statut de progression de l'export -->
                <GridViewColumn Header="Progress" Width="70">
                  <GridViewColumn.CellTemplate>
                    <DataTemplate>
                      <TextBlock Text="{Binding [Progress]}" 
                                 Foreground="{Binding [ProgressColor]}"/>
                    </DataTemplate>
                  </GridViewColumn.CellTemplate>
                </GridViewColumn>
              </GridView>
            </ListView.View>
          </ListView>
        </StackPanel>
      </Border>
      
      <!-- ═══════════════════════════════════════════════════════════════ -->
      <!-- SECTION 3 : BARRE D'ACTION (Statut + Bouton Export) -->
      <!-- ═══════════════════════════════════════════════════════════════ -->
      <DockPanel LastChildFill="False" Margin="0,8,0,0">
        <!-- Message de statut (erreurs, avertissements, succès) -->
        <TextBlock Name="ExportStatusText"
                   VerticalAlignment="Center"
                   Margin="0,0,8,0"
                   TextWrapping="Wrap"
                   Width="Auto"
                   DockPanel.Dock="Left"
                   Foreground="{DynamicResource ErrorBrush}"
                   Text="Sélectionnez un dossier de destination valide."/>
        
        <!-- Bouton principal pour lancer l'exportation -->
        <Button Name="ExportButton"
                Content="Exporter"
                Height="32"
                MinWidth="120"
                HorizontalAlignment="Right"
                DockPanel.Dock="Right"
                IsEnabled="False"/>
      </DockPanel>
    </StackPanel>
  </ControlTemplate>
</ResourceDictionary>
