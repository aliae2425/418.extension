<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource WindowBackgroundBrush}">
  <Grid Margin="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
      <ItemsControl ItemsSource="{Binding Messages}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Margin="0,0,0,6" Padding="8,6" CornerRadius="6" MaxWidth="320"
                    Background="{DynamicResource CardBackgroundBrush}"
                    BorderBrush="{DynamicResource ControlBorderBrush}"
                    BorderThickness="1"
                    HorizontalAlignment="{Binding Alignement}">
              <StackPanel>
                <TextBlock Text="{Binding Auteur}" FontSize="10"
                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                <TextBlock Text="{Binding Texte}" TextWrapping="Wrap"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <Grid Grid.Row="1" Margin="0,8,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <TextBox Grid.Column="0" MinHeight="28" Padding="6,4" VerticalContentAlignment="Center"
               Background="{DynamicResource ControlFillBrush}"
               Foreground="{DynamicResource TextPrimaryBrush}"
               BorderBrush="{DynamicResource ControlBorderBrush}"
               Text="{Binding Saisie, UpdateSourceTrigger=PropertyChanged}">
        <TextBox.InputBindings>
          <KeyBinding Key="Return" Command="{Binding EnvoyerCommand}"/>
        </TextBox.InputBindings>
      </TextBox>
      <Button Grid.Column="1" Margin="6,0,0,0" Padding="12,4" Content="Envoyer"
              Style="{DynamicResource PrimaryButtonStyle}"
              Command="{Binding EnvoyerCommand}"/>
    </Grid>
  </Grid>
</Page>
